import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonText: {
    fontSize: 21,
    color: 'rgb(0,122,255)',
    marginTop: 50,
  },
  buttonTouchable: {
    padding: 16,
  },
});

export default styles;
